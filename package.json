{
  "name": "atlas-reader",
  "private": true,
  "type": "module",
  "version": "0.1.0",
  "packageManager": "pnpm@10.33.2",
  "scripts": {
    "build": "pnpm --filter @atlas/web build",
    "contracts:check": "bash scripts/check-contracts.sh",
    "contracts:generate": "$HOME/.cargo/bin/cargo test -p atlas-domain export_bindings",
    "dev": "pnpm web:dev",
    "format": "prettier --write . && $HOME/.cargo/bin/cargo fmt --all",
    "format:check": "prettier --check . && $HOME/.cargo/bin/cargo fmt --all -- --check",
    "lint": "eslint . --max-warnings=0",
    "rust:check": "$HOME/.cargo/bin/cargo check --workspace",
    "rust:lint": "$HOME/.cargo/bin/cargo clippy --workspace --all-targets -- -D warnings",
    "rust:test": "$HOME/.cargo/bin/cargo test --workspace",
    "test": "pnpm --filter @atlas/web test",
    "typecheck": "pnpm --filter @atlas/web typecheck",
    "web:build": "pnpm --filter @atlas/web build && $HOME/.cargo/bin/cargo build -p atlas-web --release && node scripts/package-web-assets.mjs && bash scripts/sign-local-macos-server.sh",
    "web:dev": "pnpm web:build && target/release/atlas-web",
    "web:run": "target/release/atlas-web",
    "web:start": "pnpm web:build && pnpm web:run",
    "validate": "pnpm format:check && pnpm contracts:check && pnpm lint && pnpm typecheck && pnpm test && pnpm build && pnpm rust:lint && pnpm rust:test"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "eslint": "^10.7.0",
    "eslint-plugin-react-hooks": "^7.1.1",
    "eslint-plugin-react-refresh": "^0.5.3",
    "globals": "^17.3.0",
    "prettier": "^3.9.6",
    "typescript-eslint": "^8.65.0"
  }
}
